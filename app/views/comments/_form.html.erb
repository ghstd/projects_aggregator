<%= form_with model: comment do |f| %>
  <%= render 'shared/errors', object: comment %>

  <%= f.text_area :text, autofocus: true %>
  <%= f.submit t("buttons.save"), class: "button_small" %>
<% end %>