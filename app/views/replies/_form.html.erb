<%= form_with model: [comment, reply] do |f| %>
  <%= render 'shared/errors', object: reply %>

  <%= f.text_area :text, autofocus: true %>
  <%= f.submit t("buttons.save"), class: params[:action] == "edit" ? "button_small" : "button_micro" %>
<% end %>